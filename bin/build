#!/bin/bash

#docker ps -q -a | xargs docker rm
#docker images -q --filter "dangling=true" | xargs docker rmi

docker build -t madness .

docker run --rm -it -v $PWD:/workdir madness /bin/bash -c "python3 -m pip freeze > /workdir/requirements.txt"